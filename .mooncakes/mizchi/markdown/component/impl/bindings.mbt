// Generated by moon-component
// DO NOT EDIT - this file is regenerated

///| Re-export cabi_realloc for wasm linking
pub fn cabi_realloc(
  old_ptr : Int,
  old_size : Int,
  align : Int,
  new_size : Int,
) -> Int {
  @cabi.cabi_realloc(old_ptr, old_size, align, new_size)
}

pub fn wasmExportMdSerialize(source_ptr : Int, source_len : Int) -> Int {
  let source = @cabi.cabi_lift_string(source_ptr, source_len)
  let result = md_serialize(source)
  let (ptr, len) = @cabi.cabi_lower_string(result)
  let retptr = @cabi.cabi_realloc(0, 0, 4, 8)
  @cabi.cabi_write_i32(retptr, ptr)
  @cabi.cabi_write_i32(retptr + 4, len)
  retptr
}

pub fn wasmExportMdToAstJson(source_ptr : Int, source_len : Int) -> Int {
  let source = @cabi.cabi_lift_string(source_ptr, source_len)
  let result = md_to_ast_json(source)
  let (ptr, len) = @cabi.cabi_lower_string(result)
  let retptr = @cabi.cabi_realloc(0, 0, 4, 8)
  @cabi.cabi_write_i32(retptr, ptr)
  @cabi.cabi_write_i32(retptr + 4, len)
  retptr
}

pub fn wasmExportMdToHtml(source_ptr : Int, source_len : Int) -> Int {
  let source = @cabi.cabi_lift_string(source_ptr, source_len)
  let result = md_to_html(source)
  let (ptr, len) = @cabi.cabi_lower_string(result)
  let retptr = @cabi.cabi_realloc(0, 0, 4, 8)
  @cabi.cabi_write_i32(retptr, ptr)
  @cabi.cabi_write_i32(retptr + 4, len)
  retptr
}

///|
fn main {
  ()
}
