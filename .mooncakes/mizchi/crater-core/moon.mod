name = "mizchi/crater-core"

version = "0.19.0"

import {
  "mizchi/css@0.7.3",
}

repository = "https://github.com/mizchi/crater"

license = "Apache-2.0"

description = "Layout output types (Layout, LayoutResult, LayoutContext, MeasureFunc, ...) and Node tree for crater. Depends on mizchi/css for value types."

preferred_target = "js"
