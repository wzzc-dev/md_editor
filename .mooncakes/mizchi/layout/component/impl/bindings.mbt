// Generated by moon-component
// DO NOT EDIT - this file is regenerated

///|
pub(all) enum AlignSelf {
  Auto
  Start
  End
  Center
  Stretch
  Baseline
} derive(Show, Eq)

///|
pub fn AlignSelf::from_ordinal(ord : Int) -> AlignSelf {
  match ord {
    0 => Auto
    1 => Start
    2 => End
    3 => Center
    4 => Stretch
    5 => Baseline
    _ => abort("invalid ordinal")
  }
}

///|
pub fn AlignSelf::ordinal(self : AlignSelf) -> Int {
  match self {
    Auto => 0
    Start => 1
    End => 2
    Center => 3
    Stretch => 4
    Baseline => 5
  }
}

///|
pub(all) enum Alignment {
  Start
  End
  FlexStart
  FlexEnd
  Center
  SpaceBetween
  SpaceAround
  SpaceEvenly
  Stretch
  Baseline
} derive(Show, Eq)

///|
pub fn Alignment::from_ordinal(ord : Int) -> Alignment {
  match ord {
    0 => Start
    1 => End
    2 => FlexStart
    3 => FlexEnd
    4 => Center
    5 => SpaceBetween
    6 => SpaceAround
    7 => SpaceEvenly
    8 => Stretch
    9 => Baseline
    _ => abort("invalid ordinal")
  }
}

///|
pub fn Alignment::ordinal(self : Alignment) -> Int {
  match self {
    Start => 0
    End => 1
    FlexStart => 2
    FlexEnd => 3
    Center => 4
    SpaceBetween => 5
    SpaceAround => 6
    SpaceEvenly => 7
    Stretch => 8
    Baseline => 9
  }
}

///|
pub(all) struct Pair {
  a : Double
  b : Double
} derive(Show, Eq)

///| Re-export cabi_realloc for wasm linking
pub fn cabi_realloc(
  old_ptr : Int,
  old_size : Int,
  align : Int,
  new_size : Int,
) -> Int {
  @cabi.cabi_realloc(old_ptr, old_size, align, new_size)
}

pub fn wasmExportApplyAspectRatio(width : Double, height : Double, has_ratio : Int, ratio_value : Double, width_changed : Int, height_changed : Int) -> Int {
  let result = apply_aspect_ratio(width, height, has_ratio != 0, ratio_value, width_changed != 0, height_changed != 0)
  let retptr = @cabi.cabi_realloc(0, 0, 4, 16)
  // Lower record to linear memory
      @cabi.cabi_write_f64(retptr + 0, result.a)
      @cabi.cabi_write_f64(retptr + 8, result.b)
  retptr
}

pub fn wasmExportClampSize(width : Double, height : Double, min_width : Double, max_width : Double, min_height : Double, max_height : Double) -> Int {
  let result = clamp_size(width, height, min_width, max_width, min_height, max_height)
  let retptr = @cabi.cabi_realloc(0, 0, 4, 16)
  // Lower record to linear memory
      @cabi.cabi_write_f64(retptr + 0, result.a)
      @cabi.cabi_write_f64(retptr + 8, result.b)
  retptr
}

pub fn wasmExportClampSizeWithBoxMin(width : Double, height : Double, min_width : Double, max_width : Double, min_height : Double, max_height : Double, box_min_width : Double, box_min_height : Double) -> Int {
  let result = clamp_size_with_box_min(width, height, min_width, max_width, min_height, max_height, box_min_width, box_min_height)
  let retptr = @cabi.cabi_realloc(0, 0, 4, 16)
  // Lower record to linear memory
      @cabi.cabi_write_f64(retptr + 0, result.a)
      @cabi.cabi_write_f64(retptr + 8, result.b)
  retptr
}

pub fn wasmExportClampValue(value : Double, min_val : Double, max_val : Double) -> Double {
  clamp_value(value, min_val, max_val)
}

pub fn wasmExportComputeAlignOffset(align : Int, remaining_space : Double) -> Double {
  let align : Alignment = Alignment::from_ordinal(align)
  compute_align_offset(align, remaining_space)
}

pub fn wasmExportComputeAlignSelfOffset(self_align : Int, parent_align : Int, remaining_space : Double) -> Double {
  let self_align : AlignSelf = AlignSelf::from_ordinal(self_align)
  let parent_align : Alignment = Alignment::from_ordinal(parent_align)
  compute_align_self_offset(self_align, parent_align, remaining_space)
}

pub fn wasmExportComputeJustify(align : Int, container_size : Double, item_count : Int) -> Int {
  let align : Alignment = Alignment::from_ordinal(align)
  let result = compute_justify(align, container_size, item_count)
  let retptr = @cabi.cabi_realloc(0, 0, 4, 16)
  // Lower record to linear memory
      @cabi.cabi_write_f64(retptr + 0, result.a)
      @cabi.cabi_write_f64(retptr + 8, result.b)
  retptr
}

pub fn wasmExportResolveAlignSelf(self_align : Int, parent_align : Int) -> Int {
  let self_align : AlignSelf = AlignSelf::from_ordinal(self_align)
  let parent_align : Alignment = Alignment::from_ordinal(parent_align)
  resolve_align_self(self_align, parent_align).ordinal()
}

pub fn wasmExportResolveAutoMarginPair(start_auto : Int, end_auto : Int, margin_start : Double, margin_end : Double, remaining_space : Double) -> Int {
  let result = resolve_auto_margin_pair(start_auto != 0, end_auto != 0, margin_start, margin_end, remaining_space)
  let retptr = @cabi.cabi_realloc(0, 0, 4, 16)
  // Lower record to linear memory
      @cabi.cabi_write_f64(retptr + 0, result.a)
      @cabi.cabi_write_f64(retptr + 8, result.b)
  retptr
}

///|
fn main {
  ()
}
