import {
  "moonbitlang/moon_config" @config,
  "moonbitlang/async",
  "moonbitlang/async/fs",
  "moonbitlang/async/stdio",
  "moonbitlang/async/io",
  "moonbitlang/x/sys",
  "moonbitlang/core/json",
}

supported_targets = "+native"

pkgtype(kind: "executable")
